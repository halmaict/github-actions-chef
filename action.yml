name: 'Chef Delivery Action'
author: 'Jason Field'
description: 'Run Chef delivery on your cookbooks with ease'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'tool'
  color: 'red'
inputs:
  cookbooks_dir:
    description: 'Name of the cookbooks directory'
    required: false
    default: 'cookbooks'
  gems:
    description: 'String of gems to install'
    required: false
    default: ''
